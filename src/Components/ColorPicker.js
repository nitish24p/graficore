import React from 'react';
import ActionButton from './ActionButton.js';

const ColorPicker = () => {
	return (
		<div>
      <ActionButton label='Add A Color' color='teal' selector='add-color-picker'/>
    </div>
	)
}

export default ColorPicker